<ul class="breadcrumb-mini">
  <%= render :partial => "help_central/breadcrumb/search" %>
</ul>

<div class="content-body">
  <%= render partial: "shared/big_search_form", locals: { question: "Procurando por algo?" } %>

  <div class="search-results help-central-section">

    <p>Você buscou por <b>“<%= params[:search] %>”</b>. Achamos <b><%= @search.total %></b> resultados.</p>

    <ul class="questions-list">
      <div id="search-results">
        <%= render partial: @results, locals: { with_path: true, search: true } %>
      </div>
    </ul>
  </div>

  <hr class="list-separator">

  <div class="help-central-footer">
    <div class="pagination">
      <%= paginate @results, remote: true, window: 3 %>
    </div>
  </div>

</div>

<ul class="breadcrumb-mini white-area">
  <%= render :partial => "help_central/breadcrumb/search" %>
</ul>

